# Local praxis checkout the build script resolves, and build output.
# The policy engine is not here: praxis pins it by git revision, so cargo fetches
# it rather than reading a sibling path.
/.praxis
# ... and any hand-made backup of it, which is a git checkout too and would
# otherwise be staged as an embedded repository.
/.praxis.*
/target
